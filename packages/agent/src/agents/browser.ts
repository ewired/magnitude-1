/**
 * Browser Agent Definition
 *
 * Visual browser interaction agent. Has browser tools + think.
 * Receives automatic screenshots before each turn.
 *
 * NOTE: Browser agent is currently inactive and not imported anywhere.
 * Imports are commented out to avoid typecheck errors until browser
 * support is re-integrated.
 */

// Browser agent is currently disabled. The old role API (defineRole, observe,
// idle, defineThinkingLens) no longer exists in @magnitudedev/roles.
// This file is preserved for future re-integration but is not compiled into
// the active agent.

export {}
